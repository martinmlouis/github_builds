apiVersion: v1
kind: Service
metadata:
  name: {{ include "nignx.fullname" . }}
  labels:
    {{- include "nignx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nignx.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nignx.fullname" . }}-test-connection"
  labels:
    {{- include "nignx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nignx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
